create table users(
    id serial primary key,
    user_name varchar(255),
    first_name varchar(255),
    last_name varchar(255),
    email varchar(255),
    img varchar(255),
    auth_id varchar(255)
)